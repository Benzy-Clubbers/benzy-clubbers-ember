{{page-title "Benzy Clubbers"}}
<header>
    <BsNavbar @type="dark" @backgroundColor="dark" @collapsed={{true}} @onCollapse={{action (mut this.collapsed) true}}
        @onExpand={{action (mut collapsed) false}} as |navbar|>
        <div class="navbar-header">
            {{navbar.toggle}}
            {{#link-to "index" class="navbar-brand"}}Benzy Clubbers{{/link-to}}
        </div>
        <navbar.content>
            <navbar.nav as |nav|>
                <nav.dropdown as |dd|>
                    <dd.toggle>Dropdown <span class="caret"></span></dd.toggle>
                    <dd.menu as |ddm|>
                        <ddm.item>
                            <ddm.link-to @route="index">Home</ddm.link-to>
                        </ddm.item>

                    </dd.menu>
                </nav.dropdown>
            </navbar.nav>
        </navbar.content>
    </BsNavbar>
</header>

<main class="car-details">
    {{outlet}}
</main>